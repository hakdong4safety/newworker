아래 내용을 전부 복사해서

  Firebase 콘솔 → Firestore Database → 규칙(Rules) 탭
  → 기존 내용 전부 지우고 → 붙여넣기 → [게시] 버튼 클릭

게시 버튼을 누르지 않으면 반영되지 않습니다.

════════════════════════════════════════════════════════════

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function signedIn() { return request.auth != null; }
    function known()  { return signedIn()
        && exists(/databases/$(database)/documents/users/$(request.auth.uid)); }
    function me()     { return get(/databases/$(database)/documents/users/$(request.auth.uid)).data; }
    function ok()     { return known() && me().approved == true; }
    function isGC()   { return ok() && me().role == 'gc'; }
    function myCo()   { return ok() ? me().company : '__none__'; }

    // ── 근로자 제출 기록 (1명당 문서 1건) ──
    match /records/{id} {
      function okRecord() {
        return request.resource.data.keys().hasOnly(['json','updatedAt','company','dateKey'])
            && request.resource.data.json is string
            && request.resource.data.json.size() < 1000000
            && request.resource.data.company is string;
      }
      allow create: if okRecord();                     // 근로자는 로그인 없이 제출만
      allow read:   if isGC() || (ok() && resource.data.company == myCo());
      allow update: if okRecord()
                    && (isGC() || (ok() && resource.data.company == myCo()));
      allow delete: if isGC();
    }

    // ── 원청 자리 (딱 한 번만 만들어짐) ──
    match /meta/bootstrap {
      allow read:   if true;
      allow create: if signedIn() && request.resource.data.uid == request.auth.uid;
      allow update, delete: if isGC();
    }

    // ── 계정 ──
    match /users/{uid} {
      allow read: if isGC() || (signedIn() && request.auth.uid == uid);
      allow create: if signedIn() && request.auth.uid == uid && (
           (request.resource.data.role == 'sub'
            && request.resource.data.approved == false)
        || (request.resource.data.role == 'gc'
            && exists(/databases/$(database)/documents/meta/bootstrap)
            && get(/databases/$(database)/documents/meta/bootstrap).data.uid == request.auth.uid)
      );
      allow update, delete: if isGC();
    }

    // ── 양식 설정 · 협력사 목록 · 순번 카운터 ──
    // 근로자 화면도 읽어야 하므로 읽기는 열어 둔다. 개인정보는 들어가지 않는다.
    match /kv/{id} {
      allow read:  if true;
      allow write: if isGC() || id.matches('seq-.*') || id == '__diag';
    }
  }
}

════════════════════════════════════════════════════════════
지금 쓰고 계신 규칙과 무엇이 다른가
════════════════════════════════════════════════════════════

1) users 블록이 없었습니다
   → 로그인은 되지만 계정 정보를 못 읽어 튕깁니다. (지금 증상)

2) meta 블록이 없었습니다
   → 원청 계정을 만들 수 없고 "이미 원청 계정이 있습니다"로 잘못 뜹니다.

3) hasOnly(['json','updatedAt']) 였습니다   ★ 가장 급함
   → 새 코드는 company · dateKey 도 함께 저장합니다.
      (규칙이 "누구 회사 사람인지"를 읽어야 협력사별로 막을 수 있습니다)
   → 옛 규칙 그대로 두면 근로자가 서류를 아예 저장하지 못합니다.

4) read: if true / delete: if true 였습니다
   → 주소만 알면 누구나 전원의 신분증 사진과 건강정보를 읽고
      지울 수 있는 상태였습니다. 이제 원청과 소속 협력사만 됩니다.

════════════════════════════════════════════════════════════
붙여넣은 뒤 순서
════════════════════════════════════════════════════════════

1. Authentication → 시작하기 → 로그인 방법 → 이메일/비밀번호 켜기
2. Authentication → 설정 → 승인된 도메인 → 본인의 xxx.github.io 추가
3. 앱 접속 → 강력 새로고침(Ctrl+F5) → [관리자]
   → 맨 아래 "현장을 처음 여시나요 · 원청 관리자 계정 만들기"
4. 로그인 후 [설정] 탭에서 협력사 등록
5. 협력사 담당자가 가입 신청 → [계정 관리] 탭에서 승인

════════════════════════════════════════════════════════════
기존에 저장된 자료에 대해
════════════════════════════════════════════════════════════

규칙을 바꾸기 전에 저장된 기록에는 company 항목이 문서 겉면에 없습니다.
그래서 협력사 계정에는 보이지 않고, 원청에게만 보입니다.

원청으로 로그인해 그 근로자를 열고 혈압을 입력하거나 사진을 붙이면
저장되면서 자동으로 채워집니다.
건수가 많아 일일이 열기 어려우면 알려 주세요. 일괄 정비 버튼을 넣겠습니다.
